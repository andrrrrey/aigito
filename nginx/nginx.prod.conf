# Production nginx config with SSL (Let's Encrypt)
# Replace app.aigito.com with actual domain before use

# HTTP → HTTPS redirect
server {
    listen 80;
    server_name app.aigito.com www.app.aigito.com;
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }
    location / {
        return 301 https://$host$request_uri;
    }
}

server {
    listen 443 ssl http2;
    server_name app.aigito.com www.app.aigito.com;

    ssl_certificate     /etc/letsencrypt/live/app.aigito.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/app.aigito.com/privkey.pem;
    ssl_protocols       TLSv1.2 TLSv1.3;
    ssl_ciphers         HIGH:!aNULL:!MD5;

    # Security headers
    add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
    add_header X-Frame-Options DENY always;
    add_header X-Content-Type-Options nosniff always;

    # Health check → backend
    location = /health {
        proxy_pass http://backend:8000;
    }

    # Uploaded avatar images → backend
    location /uploads/avatars/ {
        proxy_pass http://backend:8000;
        proxy_set_header Host $host;
    }

    # API → FastAPI backend
    location /api/ {
        proxy_pass http://backend:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;
        proxy_read_timeout 120s;
        client_max_body_size 20M;
    }

    # LiveKit WebSocket — optimized for real-time video signaling
    # Note: livekit runs with network_mode:host, use host-gateway alias
    location /rtc/ {
        proxy_pass http://livekit:7880/;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_read_timeout 3600s;
        proxy_send_timeout 3600s;
        proxy_buffering off;
        proxy_cache off;
    }

    # Admin panel
    location /admin/ {
        alias /usr/share/nginx/html/admin/;
        try_files $uri $uri/ /admin/index.html;
        expires 1h;
    }

    # Kiosk
    location /kiosk/ {
        alias /usr/share/nginx/html/kiosk/;
        try_files $uri $uri/ /kiosk/index.html;
        expires 1h;
    }

    # Default
    location / {
        root /usr/share/nginx/html/admin;
        try_files $uri $uri/ /index.html;
    }
}
